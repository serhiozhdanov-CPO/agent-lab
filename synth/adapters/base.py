"""Контракт адаптера слоя приёма (см. canonical-format.md, раздел «Контракт адаптера»)."""

from __future__ import annotations

from typing import Any, Iterator, Protocol, runtime_checkable

from ..canonical import Observation


@runtime_checkable
class Adapter(Protocol):
    """Единый интерфейс всех источников данных.

    Обязанности реализации:
      1. привести имена к реестру метрик и значения — к каноническим единицам;
      2. проставить method и method_detail: пара (metric, method_detail) —
         условие сопоставимости, и без неё значение попадёт в чужой ряд;
      3. вычислить effective_date в локальном поясе субъекта по правилу
         атрибуции суток (сон относится ко дню пробуждения);
      4. НЕ заполнять пропуски: отсутствующее данное не порождает строки;
      5. проставить source и, если источник её сообщает, quality.confidence;
      6. заявить в capabilities() только те метрики, которые источник
         действительно умеет отдавать.

    Чего адаптер делать не должен: сглаживать, усреднять, отбрасывать
    выбросы, разрешать конфликты между вендорами. Это решения с потерей
    информации, и принимаются они потребителем под конкретную задачу.
    """

    name: str
    adapter_version: str

    def capabilities(self) -> set[str]:
        """Метрики реестра, которые этот источник в принципе умеет отдавать."""
        ...

    def read(self, raw: Any) -> Iterator[Observation]:
        """Разобрать сырьё источника в поток канонических наблюдений."""
        ...
